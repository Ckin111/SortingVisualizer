import React from 'react';
import myNode from './node'


function quicksort(nodes: myNode[]): myNode[] {
    if (nodes.length <= 1) {
        return (nodes)
    }
    return nodes;
}

export default quicksort;